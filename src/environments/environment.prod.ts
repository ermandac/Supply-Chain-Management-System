export const environment = {
  production: true,
  apiUrl: '/api', // This will be relative to the domain in production
  defaultPageSize: 10,
  tokenKey: 'auth_token',
  refreshTokenInterval: 3600000,
};
